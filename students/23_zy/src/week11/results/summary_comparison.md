# Week 11 Task C：模拟数据与 Kaggle 真实数据对比总结

## 1. 指标对比

| dataset          |   mean_RMSE |   mean_MAE |   mean_MAPE |
|:-----------------|------------:|-----------:|------------:|
| synthetic        |     20.3558 |    15.8626 |     3.31211 |
| kaggle_insurance |   5929.06   |  4147.6    |    41.631   |

## 2. 为什么模拟数据中的推测更容易？

在模拟数据中，我知道数据是怎么被生成出来的，也知道每个变量在真实公式中的方向和大致作用。因此，当模型估计出系数之后，我可以直接把结果和 DGP 进行比较。如果结果不一致，我也能大致判断是噪声、异常值、共线性还是预处理造成的。

## 3. 为什么真实数据解释更困难？

真实 Kaggle 数据并没有告诉我们真正的数据生成机制。例如医疗费用受到年龄、BMI、吸烟状态等因素影响，但也可能受到疾病史、保险类型、地区医疗价格等未观测因素影响。这些变量不在数据中，模型就无法直接控制它们。所以即使模型分数还可以，解释时也要注意：这更多是相关关系，而不是严格因果关系。

## 4. 共线性、缺失值、异常值的影响差异

在模拟数据中，共线性是我主动设计的，因此它的来源比较清楚。而在真实数据中，共线性可能来自变量之间的自然关系，例如年龄、身体状况和医疗费用之间可能本来就有关联。缺失值和异常值在模拟数据中是可控的，但在真实数据中，它们可能反映数据采集过程中的真实问题，因此处理时不能只追求模型分数，还要考虑业务合理性。

## 5. 为什么无泄露交叉验证很重要？

如果先对全体数据做缺失值填补、标准化或异常值处理，再切分训练集和验证集，验证集的信息就会提前进入训练过程，这就是数据泄露。数据泄露会让模型评估结果看起来更好，但上线后面对新数据时效果可能下降。本次代码在每一折中都只用训练集拟合填补器和标准化器，然后再处理验证集，所以可以减少数据泄露问题。

## 6. 自己维护 utils 思想的作用

这周虽然只写了一个 `main.py`，但代码中延续了前几周的组件化思想。例如缺失值填补、标准化、OLS 模型、评估指标和 VIF 诊断都被拆成独立函数或类。这样做的好处是，以后换一个数据集时，不需要重新写整套流程，只需要调整目标变量、清洗规则和报告解释即可。

## 7. 总结

通过这次作业，我把模拟数据和真实数据放在同一个回归分析框架下进行比较。模拟数据适合理解模型是否能恢复已知机制，真实数据则更接近实际业务问题。两者结合起来，可以帮助我更清楚地理解回归分析中的预测、推测、数据泄露和模型解释问题。
