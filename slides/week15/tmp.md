## chapter 1

1. 需要在合适的地方加上：
  - 我们要么用线性模型直接拟合 $y \in {0,1}$,显然是不行的；
  - 要么把连续回归出来的 y 当概率。

  当然你也可以只把这句话放到 lecture 里。

2. 再第一章末尾的适当位置，增加一个回顾：
  - 分类问题和回归问题的本质区别就是`连续 vs 离散`；
  - 而我们在“独热编码”的时候已经见识到这个区别造成的巨大差别，只是当时是在 X 的范畴内。


## chapter 2

1. 请遍历 py 文件，把所有 latex code 的环境从 `something` 变成 $something$;
2. 正如上文说的`输入可以是任意实数`可能并不完全，不如改成对输入没有额外限制；
3. 在 `本幕主图` 这一页，加上 sigmoid 函数的数学定义会比较好。

## chapter 3

1. 这里我觉得我们应该先讨论一个 dummy senario: 那就是为什么不能用 RMSE 作为 loss function；
2. fig 这里需要在 lecture.md 中从“纵轴”和“图像含义”两个方面好好解释；
3. 我觉得你的逻辑是对的，“为什么转向 波努力似然 “是一个非常本质的提问；但是我们的 fig 或者说我们的展示逻辑，并没有清晰的回答这个问题；
  - 也许需要增加子问题；
  - 也许需要数学公式；
  - 也许需要其他串词或者说明，
  来更好地回答这个问题。
4. 同理，我们讨论了半天的 log loss 性质，但在最后才知道 likelihood 是波努力分布；los 是对应的最大似然。
  - 也许从 @ipynb_agent_spec.md 角度就要修改 --- 虽然直观很重要，但明确的定义有时候不能出现的过晚。

## chapter 4

1. 这里的设问有类似的问题，“你直观上觉得” 之后的内容，是不对吗？学生在第一次阅读的时候肯定会疑惑；我们应该暗示的更明显一点，这种直观的判断是错误的。
  - 只有这样，我们才能把注意力放在下一页里；不然我们不知道这两页的两个问题究竟有什么关系。
2. 同上，这里的讨论了半天的 odds 和 log-odds，又是图像又是性质，但我们只在结束的时候才知道我们讨论的对象是谁，太反直观了，一定要修改 md 里的流程；讨论到新定义，尤其是新定义的性质时，提早呈现定义的具体形式。

## chapter 5

1. 同样，先给出各个评价标准的定义，然后在看他们是如何衡量分类效果的。
2. 给出 混淆矩阵、一级指标和二级指标的定义 与 性质。