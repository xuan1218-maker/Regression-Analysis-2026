## Intro

- PCA PCR for what?
- 串词：我们的信息不足以对每一个 列 都产生足够准确的估计；具体原因要么是 `p > n`，要么是 `共线性`。


## Scene 1 High dimension

### Page1

- 极端例子：一元回归（直线），训练集只有一个点 `n = 1`，误差铁定是 0 ； 但这个模型没有任何泛化能力。

### Page3: Fig

- 左图，如何解读呢？
  - 这里 test RMSE 的起伏是否是随机？为什么会有 bump 和下降？
  - train RMSE 显示，虚假的低误差。
- 右图：
  - 是否应该给出背景信息呢？（例如 n = 60，还是这里有别的原因导致了 rank 到了极限60？）

### Page4

- 什么是 condition number？定义、定义式和直观分别是什么？
- “自由度”怎么理解？

### Page5

- 虽然这里矩阵乘法和 rank 的关系很简单，但还是写一点串词比较好。

## Scene 2 Multi-linear

### Page1 

- 回忆一下 Ridge 做了什么，为什么 Ridge 更偏向“稳定划分功劳”？写一点串词。

### Page3 Fig

- 关注对象：方差在前后的对比（是不是也该加点背景知识？虽然能一眼看出来前三个量明显共线）
- “高维和共线性常常是同一类麻烦的两面：变量一多，重复信息就更容易出现。”这句话对吗？高维的问题是 每一列平均获取的有效信息减少，这里和“重复信息”只是相同的表现，而不是相同的原因吧？

## Scene 3

### Page1

- 如何思考，考虑“拟合优度”的定义，为什么我们要关注波动？
- 在 `PCA 的一个关键词是` 这一页之后，是不是应该加一个 PCA 算法的数学描述或者伪代码描述？（我在本 scene 的最后看到了，出现的太晚了我觉得）
- `本幕辅图` 这一页，也可以解释成，是一个“低维平面”潜入了“高维空间”；实际的空间的自由度没那么多。


## Scene 4

### Page 3 fig

- 我该怎么解释 `PCR CV RMSE`，它自身的趋势 和与 Train test 曲线的对比分别说明了什么？
- 这个图里，为什么 ols 的 train error 能达到 0？CV 只是用来选最优 PCA 个数的吗？感觉此方法比 OLS好的有限诶？

## Scene 5

是否再加入 “前向后向变量选择” 来一同比较？

### Page 1

- “很多变量都在表达少数潜在因子”，这句话过于意义不明，作为数学语言上下文不明晰，作为自然语言不是人话。

### Page2 Fig

- 图如何读呢？（如何红蓝对比，如何红红、蓝蓝自己对比）
- Size=number，这里 size 是什么呢？平均选择的变量数吗？那需要写点实验设计的串词。
- coef_instability 是什么，给出定义

## Synthesis

- “到这里，学生应该至少带走 5 个判断”，违背了我们的约定，不要什么“学生带走”，很不自然。

## Transition

我们下周讲 “`广义线性模型（GLM）核心：逻辑回归`”，所以这里修改串词。